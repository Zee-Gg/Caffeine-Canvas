# Caffeine Canvas - Build

Build the Tailwind CSS into `dist/style.css`.

Windows (PowerShell):

```powershell
npm install
npm run build:css
```

Or to watch for changes:

```powershell
npm run watch:css
```
